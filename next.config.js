/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  allowedDevOrigins: [
    "elenora-unmelancholy-unmomentously.ngrok-free.dev",
  ],
}

module.exports = nextConfig
